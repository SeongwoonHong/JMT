{
  "name": "jmt",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "concurrently 'cd JMT-Frontend && rm -rf node_modules && npm i' 'cd JMT-Backend && rm -rf node_modules && npm i'",
    "start": "concurrently 'cd JMT-Frontend && npm start' 'cd JMT-Backend && npm start'"
  },
  "author": "seongwoon hong",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^4.0.1",
    "husky": "^0.15.0-rc.9"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "morgan": "^1.9.1"
  }
}
